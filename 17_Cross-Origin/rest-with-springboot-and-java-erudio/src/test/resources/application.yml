server:
  port: 8888
cors:
  originPatterns: http://localhost:3000,http://localhost:8090,https://erudio.com
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false # Esse deu um erro antes mas agora está normal
    hibernate:
      ddl-auto: none
   # properties:
    #  hibernate:
     #   dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
springdoc:
  pathsToMatch: /api/**/v1/**
  swagger-ui:
    use-root-path: true  